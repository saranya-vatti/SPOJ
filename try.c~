#include <stdio.h>
#include <math.h>
int main(){
	char a[1000],b[20];
	int i=0,j=0;
	scanf("%s %s",a,b);
	while(a[i++]!='\0');
	while(b[j++]!='\0');
	printf("%c %c\n",a[i-2],b[j-2]);
	return (0);
}
	
